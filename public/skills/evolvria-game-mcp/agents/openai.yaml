interface:
  display_name: "Evolvria 游戏 MCP"
  short_description: "查看 Evolvria 游戏 MCP 权限、风险和工具边界"
  default_prompt: "Use $evolvria-game-mcp to check Evolvria MCP permissions before game workspace operations."
dependencies:
  tools:
    - type: "mcp"
      value: "evolvria-game"
      description: "本地 stdio MCP 服务，运行 yarn mcp:game 或 scripts/evolvria-mcp.mjs"
      transport: "stdio"
